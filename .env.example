# This file is no longer used. Credentials are now stored securely in the system keyring.
# Run 'linear-cli setup' to configure your credentials.
#
# For reference, the following credentials are required:
# - LINEAR_API_KEY: Get from https://linear.app/settings/api
# - GOOGLE_API_KEY: Get from https://ai.google.dev/gemini-api/docs/api-key
# - LINEAR_TEAM_ID: Find in your Linear team settings